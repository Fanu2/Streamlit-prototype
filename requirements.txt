streamlit>=1.30
transformers[torch]>=4.41  # pulls in torch automatically
torch>=2.3                # the first version that ships wheels for Python 3.13
accelerate                # optional – helps with GPU/CPU placement
